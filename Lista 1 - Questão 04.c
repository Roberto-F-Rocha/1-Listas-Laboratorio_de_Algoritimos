#include <stdio.h>
#include <stdlib.h>

/*
Exercício 04. Crie um programa que leia um valor do tipo float e depois o imprima usando o
operador “%d”. Veja o que aconteceu
*/

int main(){
		float numero = 0;
		
		scanf("%f", &numero);
		
		printf("Valor lido: %d\n", numero);
  
	  system("pause");
 	 return (0);
}